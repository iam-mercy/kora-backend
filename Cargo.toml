[workspace]
resolver = "2"
members = ["crates/kora-2fa"]

[workspace.package]
edition = "2021"
license = "MIT"
rust-version = "1.75"

[workspace.lints.clippy]
# Keep the lint surface tight; CI runs `clippy -- -D warnings`.
all = "warn"
